@using System.Text.Json

<pre>@JsonSerializer.Serialize(Data, new JsonSerializerOptions { WriteIndented = Indented })</pre>

@code {
    [Parameter] public object Data { get; set; }
    [Parameter] public bool Indented { get; set; } = true;
}