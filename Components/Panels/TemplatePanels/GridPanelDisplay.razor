@inherits PanelDisplayComponent<GridPanel>

@if (CanDisplay())
{
    <MudGrid>
        @foreach (var (item, idx) in Model.Items.Select((gridItem, idx) => (gridItem, idx)))
        {
            <PanelDisplay PanelId="@item.Id" @key="@TrackByKey(item, idx)"/>
        }
    </MudGrid>
}
